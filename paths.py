from pathlib import Path

# Points to repo root (where this file lives)
REPO_ROOT = Path(__file__).parent

# Standardized paths
PROCESSED_DATA = REPO_ROOT / "data" / "processed"
RAW_DATA = REPO_ROOT / "data" / "raw"